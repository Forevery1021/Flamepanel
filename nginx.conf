user nginx;
worker_processes auto;

events {
    worker_connections 1024;
}

http {
    include       mime.types;
    default_type  application/octet-stream;

    # 优化配置
    sendfile        on;
    keepalive_timeout  65;

    # 基础安全头（P2）：防 MIME 嗅探 / 点击劫持 / 反射型 XSS
    add_header X-Content-Type-Options "nosniff" always;
    add_header X-Frame-Options "DENY" always;
    add_header Referrer-Policy "strict-origin-when-cross-origin" always;
    add_header X-XSS-Protection "0" always;

    # Ops Panel 主配置
    server {
        listen 80;
        server_name _;

        # 静态资源（前端构建产物）
        root /app/frontend/dist;
        index index.html;

        # 后端健康检查（与前端路由区分）
        location = /health {
            proxy_pass http://127.0.0.1:8080;
        }

        # Prometheus 指标：按安全基线收紧，仅允许内网/本机访问
        # （指标含进程/资源详情，公网暴露风险面大；需要公网时建议 Basic Auth 或反代层鉴权）
        location = /metrics {
            allow 127.0.0.1;
            allow ::1;
            allow 10.0.0.0/8;
            allow 172.16.0.0/12;
            allow 192.168.0.0/16;
            deny all;
            proxy_pass http://127.0.0.1:8080;
        }

        # 前端路由回退
        location / {
            try_files $uri $uri/ /index.html;
        }

        # API 代理到 Rust 后端
        location /api/ {
            proxy_pass http://127.0.0.1:8080;
            proxy_set_header Host $host;
            proxy_set_header X-Real-IP $remote_addr;
            proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
            proxy_set_header X-Forwarded-Proto $scheme;
        }

        # WebSocket 支持（终端）
        location /ws/ {
            proxy_pass http://127.0.0.1:8080;
            proxy_http_version 1.1;
            proxy_set_header Upgrade $http_upgrade;
            proxy_set_header Connection "Upgrade";
            proxy_set_header Host $host;
        }

        # 静态资源缓存
        location /assets/ {
            expires 30d;
            add_header Cache-Control "public";
        }
    }

    # ── TLS 生产示例（与 install.sh --tls 生成配置对齐）──
    # server {
    #     listen 443 ssl;
    #     server_name panel.example.com;
    #     ssl_certificate     /etc/nginx/certs/panel.crt;
    #     ssl_certificate_key /etc/nginx/certs/panel.key;
    #     ssl_protocols TLSv1.2 TLSv1.3;
    #     ssl_ciphers HIGH:!aNULL:!MD5;
    #     ssl_session_cache shared:SSL:10m;
    #     ssl_session_timeout 10m;
    #
    #     # HSTS（确认 HTTPS 全量生效后再开启，max-age 从 3600 起步逐步上调）
    #     add_header Strict-Transport-Security "max-age=31536000" always;
    #
    #     root /app/frontend/dist;
    #     index index.html;
    #     location = /health { proxy_pass http://127.0.0.1:8080; }
    #     location = /metrics { allow 127.0.0.1; deny all; proxy_pass http://127.0.0.1:8080; }
    #     location / { try_files $uri $uri/ /index.html; }
    #     location /api/ {
    #         proxy_pass http://127.0.0.1:8080;
    #         proxy_set_header Host $host;
    #         proxy_set_header X-Real-IP $remote_addr;
    #         proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    #         proxy_set_header X-Forwarded-Proto $scheme;
    #     }
    #     location /ws/ {
    #         proxy_pass http://127.0.0.1:8080;
    #         proxy_http_version 1.1;
    #         proxy_set_header Upgrade $http_upgrade;
    #         proxy_set_header Connection "Upgrade";
    #         proxy_set_header Host $host;
    #     }
    #     location /assets/ { expires 30d; add_header Cache-Control "public"; }
    # }
    #
    # # HTTP → HTTPS 跳转
    # server {
    #     listen 80;
    #     server_name panel.example.com;
    #     return 301 https://$host$request_uri;
    # }
}